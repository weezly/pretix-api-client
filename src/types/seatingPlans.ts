export interface SeatingPlan {
  id: number;
  name: string;
  background?: string;
  size_x: number;
  size_y: number;
}

export interface SeatingPlanDetails {
  name: string;
  background?: string;
  size_x: number;
  size_y: number;
}